{
  "name": "calendar-sync",
  "version": "1.0.0",
  "description": "Fetches ICS feeds and produces JSON, CSV, and ICS output files",
  "main": "sync.js",
  "scripts": {
    "sync": "node sync.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
